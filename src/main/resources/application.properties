# Username and password
spring.datasource.username = root
spring.datasource.password = tunis12

# ===============================
# ======= JPA / HIBERNATE =======
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the GITHUB_URL_PREFIX is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=create

spring.datasource.url = jdbc:mysql://95.111.19.222:2468/landlord_database
# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect
spring.datasource.hikari.maximum-pool-size=3
#This changes the server port:
##server.port=2442